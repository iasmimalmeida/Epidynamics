labels = label_number(accuracy = 0.1)  # Remover notação científica
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
###outro
library(scales)  # Para personalizar os números do eixo
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Encontrando o valor máximo dos ORs
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = c(0.1, 1, round(max_or, 1)),  # Definir os valores desejados no eixo
labels = label_number(accuracy = 0.1)  # Remover notação científica
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
###outro
library(scales)  # Para personalizar os números do eixo
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Encontrando o valor máximo dos ORs
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = c(0,0.1, 1, round(max_or, 1)),  # Definir os valores desejados no eixo
labels = label_number(accuracy = 0.1)  # Remover notação científica
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Encontrando o valor mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = c(round(min_or, 2), 0.1, 1, round(max_or, 1)),  # Definir os valores no eixo
labels = label_number(accuracy = 0.01)  # Ajuste para maior precisão
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando a escala do eixo x considerando o menor valor, 0.1, 1 e o máximo
x_breaks <- c(min_or, 0.1, 1, max_or)
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = label_number(accuracy = 0.1)
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando a escala do eixo X incluindo "0" como rótulo
x_breaks <- c(0.01, 0.1, 1, max_or)  # Não é possível colocar 0 matematicamente na escala log
x_labels <- c("0", "0.1", "1", format(round(max_or, 1), nsmall = 1))  # Adicionando "0" no rótulo
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = x_labels
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
## Episódico
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)") %>%
filter(str_detect(term, "Episódico"))  # Filtrando apenas os coeficientes relacionados ao perfil "Episódico"
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando a escala do eixo X incluindo "0" como rótulo
x_breaks <- c(0.01, 0.1, 1, max_or)  # Não é possível colocar 0 matematicamente na escala log
x_labels <- c("0", "0.1", "1", format(round(max_or, 1), nsmall = 1))  # Adicionando "0" no rótulo
# Criando o gráfico de forest plot para o perfil "Episódico"
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = x_labels
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial (Episódico)") +
theme_minimal()
model_final
## Transmmissão Persistente
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
tidy_model <- tidy_model %>%
filter(str_detect(term, "Transmissão Persistente"))  # Substituir pelo nível desejado
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando a escala do eixo X incluindo "0" como rótulo
x_breaks <- c(0.01, 0.1, 1, max_or)  # Não é possível colocar 0 matematicamente na escala log
x_labels <- c("0", "0.1", "1", format(round(max_or, 1), nsmall = 1))  # Adicionando "0" no rótulo
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = x_labels
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Exibir todos os termos disponíveis para verificar os nomes corretos
print(unique(tidy_model$term))
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)") %>%
filter(str_detect(term, "Episódico"))  # Ajuste conforme os termos listados acima
# Verificar quantos coeficientes foram encontrados
print(nrow(tidy_model))
# Só cria o gráfico se houver dados
if (nrow(tidy_model) > 0) {
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando a escala do eixo X incluindo "0" como rótulo
x_breaks <- c(0.01, 0.1, 1, max_or)  # Não é possível colocar 0 matematicamente na escala log
x_labels <- c("0", "0.1", "1", format(round(max_or, 1), nsmall = 1))  # Adicionando "0" no rótulo
# Criando o gráfico de forest plot para o perfil "Episódico"
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = x_labels
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial (Episódico)") +
theme_minimal()
} else {
print("Nenhum coeficiente encontrado para a categoria 'Episódico'. Verifique os nomes das variáveis no modelo.")
}
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Exibir todos os termos disponíveis para verificar os nomes corretos
print(unique(tidy_model$term))
# Filtrando apenas os coeficientes das variáveis preditoras associadas ao perfil "Episódico"
tidy_model_filtered <- tidy_model %>%
filter(term != "(Intercept)") %>%
filter(str_detect(term, "Episódico"))
# Verificar quantos coeficientes foram encontrados
print(nrow(tidy_model_filtered))
# Evitar gerar um gráfico vazio
if (nrow(tidy_model_filtered) > 0) {
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model_filtered$conf.low, na.rm = TRUE)
max_or <- max(tidy_model_filtered$conf.high, na.rm = TRUE)
# Criando a escala do eixo X incluindo "0" como rótulo
x_breaks <- c(0.01, 0.1, 1, max_or)
x_labels <- c("0", "0.1", "1", format(round(max_or, 1), nsmall = 1))
# Criando o gráfico de forest plot para o perfil "Episódico"
ggplot(tidy_model_filtered, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = x_labels
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial (Episódico)") +
theme_minimal()
} else {
print("Nenhum coeficiente encontrado para a categoria 'Episódico'. Verifique os nomes das variáveis no modelo.")
}
## geral
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Criando a escala do eixo X incluindo "0" como rótulo
x_breaks <- c(0.01, 0.1, 1, max_or)  # Não é possível colocar 0 matematicamente na escala log
x_labels <- c("0", "0.1", "1", format(round(max_or, 1), nsmall = 1))  # Adicionando "0" no rótulo
# Criando o gráfico de forest plot
ggplot(tidy_model, aes(x = estimate, y = term)) +
geom_point(color = "darkgreen", size = 3) +  # Pontos dos ORs
geom_errorbarh(aes(xmin = conf.low, xmax = conf.high), height = 0.2, color = "darkgreen") +  # Barras de erro
geom_vline(xintercept = 1, linetype = "dashed", color = "gray") +  # Linha vertical em OR = 1
scale_x_log10(
breaks = x_breaks,
labels = x_labels
) +
labs(x = "Odds Ratio (OR)", y = "", title = "Forest Plot - Modelo Multinomial") +
theme_minimal()
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
#Lendo
#banco_determinantes <- read_csv2("Dados/Determinantes/banco_determinantes.csv")
banco_determinantes <- read_csv2("banco_determinantes.csv")
#2010-2016
#clima_org_model_10_16_season <- read_csv("Dados/Determinantes/clima/clima_org_model_10_16_season.csv")
clima_org_model_10_16_season <- read_csv("clima_org_model_10_16_season.csv")
clima_org_model_10_16_season <- clima_org_model_10_16_season[, -1]  # Exclui a primeira coluna
colnames(clima_org_model_10_16_season)[1] <- "muni_code"  # Renomeia a nova primeira coluna
# Juntando
det_10_16 <- full_join(banco_determinantes, clima_org_model_10_16_season , by = "muni_code")
det_10_16 <- det_10_16[, !names(det_10_16) %in% c("pop22")]
## Removendo os bancos para a nálise ficar mais leve
remove(banco_determinantes)
remove(clima_org_model_10_16_season)
# Remover colunas pelo nome
det_10_16 <- det_10_16[, !names(det_10_16) %in% c("muni_code")]
det_10_16$region <- as.factor(det_10_16$region)
# removendo as linhas com Na
det_10_16 <- det_10_16%>%
filter(!if_any(where(is.numeric), is.na))
# Definir a categoria de referência para todas as variáveis categóricas
det_10_16$dengue_pattern <- relevel(det_10_16$dengue_pattern, ref = "Episódico/Epidêmico") # variável dependente
#Lendo
#banco_determinantes <- read_csv2("Dados/Determinantes/banco_determinantes.csv")
banco_determinantes <- read_csv2("banco_determinantes.csv")
#2010-2016
#clima_org_model_10_16_season <- read_csv("Dados/Determinantes/clima/clima_org_model_10_16_season.csv")
clima_org_model_10_16_season <- read_csv("clima_org_model_10_16_season.csv")
clima_org_model_10_16_season <- clima_org_model_10_16_season[, -1]  # Exclui a primeira coluna
colnames(clima_org_model_10_16_season)[1] <- "muni_code"  # Renomeia a nova primeira coluna
# Juntando
det_10_16 <- full_join(banco_determinantes, clima_org_model_10_16_season , by = "muni_code")
det_10_16 <- det_10_16[, !names(det_10_16) %in% c("pop22")]
## Removendo os bancos para a nálise ficar mais leve
remove(banco_determinantes)
remove(clima_org_model_10_16_season)
# Converter variáveis numéricas com vírgula decimal e categorizar as variáveis de texto
det_10_16 <- det_10_16 %>%
mutate(
across(where(~ all(grepl("^[0-9,.]+$", .))), ~ as.numeric(gsub(",", ".", .))),
across(where(is.character), as.factor))
# Visão geral dos dados
summary(det_10_16)
# Remover colunas pelo nome
det_10_16 <- det_10_16[, !names(det_10_16) %in% c("muni_code")]
det_10_16$region <- as.factor(det_10_16$region)
# removendo as linhas com Na
det_10_16 <- det_10_16%>%
filter(!if_any(where(is.numeric), is.na))
# Definir a categoria de referência para todas as variáveis categóricas
det_10_16$dengue_pattern <- relevel(det_10_16$dengue_pattern, ref = "Episódico/Epidêmico") # variável dependente
# Variáveis independentes
summary(det_10_16)
det_10_16$bioma <- relevel(det_10_16$bioma, ref = "mata atlantica")
det_10_16$tipo_regiao <- relevel(det_10_16$tipo_regiao , ref = "nao metropolitana")
det_10_16$koppen <- relevel(det_10_16$koppen, ref = "Aw")
summary(det_10_16)
# Modelo com tudo
multinom_model <- multinom(dengue_pattern ~ ., data = det_10_16)
summary(multinom_model)
names(det_10_16)
model_final <- multinom(formula = dengue_pattern ~ region + pop10 + thr_temp_Spring +
bioma + koppen + umid_med_Summer + thr_prec_Spring + thr_temp_Summer +
umid_med_Spring + thr_umid_Spring + precip_tot_Summer + altitude +
temp_med_Winter + tipo_regiao, data = det_10_16)
model_final
model_final <- na.omit(model_final)
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Encontrando os valores mínimo e máximo dos ORs
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
View(tidy_model)
# Extraindo os coeficientes do modelo multinomial com intervalo de confiança
tidy_model <- broom::tidy(model_final, conf.int = TRUE)
# Filtrando apenas os coeficientes das variáveis preditoras (removendo a categoria base)
tidy_model <- tidy_model %>%
filter(term != "(Intercept)")
# Convertendo os coeficientes log-OR para OR
tidy_model <- tidy_model %>%
mutate(estimate = exp(estimate), conf.low = exp(conf.low), conf.high = exp(conf.high))
# Encontrando os valores mínimo e máximo das ORs corretamente
min_or <- min(tidy_model$conf.low, na.rm = TRUE)
max_or <- max(tidy_model$conf.high, na.rm = TRUE)
# Exibir os valores corrigidos para conferência
print(paste("Min OR:", min_or))
print(paste("Max OR:", max_or))
# Extrair coeficientes e intervalos de confiança (IC 95%)
results <- broom::tidy(model_final, exponentiate = TRUE, conf.int = TRUE) %>%
rename(Variável = term, Categoria = y.level, OR = estimate, Lower_CI = conf.low, Upper_CI = conf.high) %>%
filter(Variável != "(Intercept)")  # Remover intercepto para melhor visualização
# Criar a coluna de intervalo de confiança (IC95%)
results <- results %>%
mutate(
IC95 = paste0("[", round(Lower_CI, 2), " - ", round(Upper_CI, 2), "]"),  # Formatar IC95%
OR = round(OR, 2)
) %>%
select(Variável, Categoria, OR, IC95)  # Selecionar apenas colunas relevantes
# Calcular p-valores manualmente a partir dos z-scores
z_scores <- summary(model_final)$coefficients / summary(model_final)$standard.errors
p_values <- 2 * (1 - pnorm(abs(z_scores)))
# Converter p-values em um dataframe formatado corretamente
p_values_df <- as.data.frame(p_values) %>%
tibble::rownames_to_column(var = "Variável") %>%
pivot_longer(cols = -Variável, names_to = "Categoria", values_to = "p_valor")
# Juntar ORs, IC95% e p-valores
final_results <- results %>%
left_join(p_values_df, by = c("Variável", "Categoria")) %>%
mutate(p_valor = ifelse(p_valor < 0.001, "<0.001", round(p_valor, 3)))
# **Reformatar os dados para que `dengue_pattern` fique como colunas**
tabela_larga <- final_results %>%
pivot_wider(
names_from = Categoria,  # Transformar as categorias de dengue em colunas
values_from = c(OR, IC95, p_valor),  # Usar OR, IC95% e p-valor como valores
names_glue = "{Categoria}_{.value}"  # Nome das colunas formatado corretamente
)
write.csv2(tabela_larga,"table1_10_16.csv")
# Criar tabela formatada com gt()
tabela_larga %>%
gt() %>%
tab_header(title = "Tabela 1: Modelo de Regressão Logística Multinomial",
subtitle = "Odds Ratio, Intervalo de Confiança 95% e p-valor para dengue_pattern") %>%
cols_label(Variável = "Variável") %>%
tab_spanner(label = "Epidêmico", columns = starts_with("Epidêmico_")) %>%
tab_spanner(label = "Episódico", columns = starts_with("Episódico_")) %>%
tab_spanner(label = "Sem transmissão", columns = starts_with("Sem transmissão_")) %>%
tab_spanner(label = "Transmissão Persistente", columns = starts_with("Transmissão Persistente_")) %>%
fmt_missing(columns = everything(), missing_text = "—")  # Substituir valores ausentes por traço
library(tidyverse)
library(nnet)
library(caret)
library(pscl)
library(DescTools)
library(car)
library(broom)
library(boot)
library(dplyr)
library(caret) # Para criar variáveis dummy
library(lmtest)
library(gt)
library(tidyverse)
library(nnet)
library(caret)
library(pscl)
library(DescTools)
library(car)
library(broom)
library(boot)
library(dplyr)
library(caret) # Para criar variáveis dummy
library(lmtest)
library(gt)
#Lendo
#banco_determinantes <- read_csv2("Dados/Determinantes/banco_determinantes.csv")
banco_determinantes <- read_csv2("banco_determinantes_patt.csv")
#2017-2022
#clima_org_model_17_22_season <- read_csv("Dados/Determinantes/clima/clima_org_model_17_22_season.csv")
clima_org_model_17_22_season <- read_csv("clima_org_model_17_22_season.csv")
clima_org_model_17_22_season <- clima_org_model_17_22_season[, -1]  # Exclui a primeira coluna
colnames(clima_org_model_17_22_season)[1] <- "muni_code"  # Renomeia a nova primeira coluna
# Juntando
det_17_22 <- full_join(banco_determinantes, clima_org_model_17_22_season , by = "muni_code")
det_17_22  <- det_17_22 [, !names(det_17_22) %in% c("pop10")]
## Removendo os bancos para a nálise ficar mais leve
remove(banco_determinantes)
remove(clima_org_model_17_22_season)
# Converter variáveis numéricas com vírgula decimal e categorizar as variáveis de texto
det_17_22 <- det_17_22 %>%
mutate(
across(where(~ all(grepl("^[0-9,.]+$", .))), ~ as.numeric(gsub(",", ".", .))),
across(where(is.character), as.factor))
# Visão geral dos dados
summary(det_17_22)
# Remover colunas pelo nome
det_17_22 <- det_17_22[, !names(det_17_22) %in% c("muni_code")]
det_17_22$region <- as.factor(det_17_22$region)
# removendo as linhas com Na
det_17_22 <- det_17_22%>%
filter(!if_any(where(is.numeric), is.na))
# Definir a categoria de referência para todas as variáveis categóricas
det_17_22$dengue_pattern <- relevel(det_17_22$dengue_pattern, ref = "Episódico/Epidêmico") # variável dependente
# Variáveis independentes
summary(det_17_22)
det_17_22$bioma <- relevel(det_17_22$bioma, ref = "mata atlantica")
det_17_22$tipo_regiao <- relevel(det_17_22$tipo_regiao , ref = "nao metropolitana")
det_17_22$koppen <- relevel(det_17_22$koppen, ref = "Aw")
summary(det_17_22)
# Modelo com tudo
multinom_model <- multinom(dengue_pattern ~ ., data = det_17_22)
summary(multinom_model)
model_final <- modelo <- multinom(
formula = dengue_pattern ~ region + pop22 + dengue_pattern_2010_2016 +
thr_temp_Spring + bioma + precip_tot_Autumn + altitude +
thr_temp_umid_Autumn + precip_tot_Winter + thr_prec_Summer,
data = det_17_22
)
model_final <- na.omit(model_final)
# previsoes
previsoes <- predict(model_final, newdata = det_17_22)
# matriz de confusão comparando as previsões com os valores reais
matriz_confusao <- confusionMatrix(previsoes, det_17_22$dengue_pattern)
print(matriz_confusao)
# Resíduos deviance
residuals_deviance <- residuals(model_final, type = "deviance")
plot(residuals_deviance, main = "Resíduos Deviance", ylab = "Resíduos", xlab = "Observações")
abline(h = 0, col = "red")
model_final
# Resíduos deviance
residuals_deviance <- residuals(model_final, type = "deviance")
plot(residuals_deviance, main = "Resíduos Deviance", ylab = "Resíduos", xlab = "Observações")
abline(h = 0, col = "red")
## Modelo nulo
m1 <- multinom(formula = dengue_pattern ~ region + pop10 , data = det_17_22)
View(det_17_22)
## Modelo nulo
m1 <- multinom(formula = dengue_pattern ~ region + pop22 , data = det_17_22)
m1
m1 <- na.omit(m1)
# previsoes
previsoes_m1 <- predict(m1, newdata = det_17_22)
# matriz de confusão comparando as previsões com os valores reais
matriz_confusao_m1 <- confusionMatrix(previsoes_m1, det_17_22$dengue_pattern)
print(matriz_confusao_m1)
# Resíduos deviance
residuals_deviance_m1 <- residuals(m1, type = "deviance")
plot(residuals_deviance_m1, main = "Resíduos Deviance", ylab = "Resíduos", xlab = "Observações")
abline(h = 0, col = "red")
m2 <- multinom(formula = dengue_pattern ~ region + pop22 + dengue_pattern_2010_2016 +
bioma + altitude, data = det_17_22)
m2
m2 <- na.omit(m2)
# previsoes
previsoes_m2 <- predict(m2, newdata = det_17_22)
# matriz de confusão comparando as previsões com os valores reais
matriz_confusao_m2 <- confusionMatrix(previsoes_m2, det_17_22$dengue_pattern)
print(matriz_confusao_m2)
# Resíduos deviance
residuals_deviance_m2 <- residuals(m2, type = "deviance")
plot(residuals_deviance_m2, main = "Resíduos Deviance", ylab = "Resíduos", xlab = "Observações")
abline(h = 0, col = "red")
m3 <- multinom(formula = dengue_pattern ~ region + pop22 + thr_temp_Spring +
precip_tot_Autumn +  thr_temp_umid_Autumn + precip_tot_Winter
+ thr_prec_Summer, data = det_17_22
)
m3
m3 <- na.omit(m3)
# previsoes
previsoes_m3 <- predict(m3, newdata = det_17_22)
# matriz de confusão comparando as previsões com os valores reais
matriz_confusao_m3 <- confusionMatrix(previsoes_m3, det_17_22$dengue_pattern)
print(matriz_confusao_m3)
# Resíduos deviance
residuals_deviance_m3 <- residuals(m3, type = "deviance")
plot(residuals_deviance_m3, main = "Resíduos Deviance", ylab = "Resíduos", xlab = "Observações")
abline(h = 0, col = "red")
m3 <- multinom(formula = dengue_pattern ~ region + pop22 + + dengue_pattern_2010_2016 + thr_temp_Spring + precip_tot_Autumn +  thr_temp_umid_Autumn + precip_tot_Winter
+ thr_prec_Summer, data = det_17_22
)
m3
m3 <- na.omit(m3)
# previsoes
previsoes_m3 <- predict(m3, newdata = det_17_22)
# matriz de confusão comparando as previsões com os valores reais
matriz_confusao_m3 <- confusionMatrix(previsoes_m3, det_17_22$dengue_pattern)
print(matriz_confusao_m3)
# Resíduos deviance
residuals_deviance_m3 <- residuals(m3, type = "deviance")
plot(residuals_deviance_m3, main = "Resíduos Deviance", ylab = "Resíduos", xlab = "Observações")
abline(h = 0, col = "red")
